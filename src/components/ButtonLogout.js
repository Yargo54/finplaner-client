import React, { Component } from "react";

import "./ButtonLogout.css"

export default class ButtonLogout extends Component {

    render() {
        return (
            <button className="button-logout">Зарегистрироваться</button>
        );
    }
}